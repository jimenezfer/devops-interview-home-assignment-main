apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-assistant.fullname" . }}-db-secret
  labels:
    {{- include "ai-assistant.labels" . | nindent 4 }}
  annotations:
    # This secret will be populated by the setup-secrets.sh script
    # from AWS Secrets Manager
    helm.sh/resource-policy: keep
type: Opaque
data: {}
